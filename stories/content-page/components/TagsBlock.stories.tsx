import TagsBlock from "../../../components/content-page/components/TagsBlock";

import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof TagsBlock> = {
  title: "content-page/components/TagsBlock",
  component: TagsBlock,
};

export default meta;
type Story = StoryObj<typeof TagsBlock>;

export const Primary: Story = {};
1;
